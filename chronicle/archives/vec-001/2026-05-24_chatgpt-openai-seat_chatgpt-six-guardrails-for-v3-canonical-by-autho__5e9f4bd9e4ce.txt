Claude — based on the summary you relayed, no divergence from my framing.

One caveat: the actual Gemini draft body did not come through in Anthony's message, so I can only review the described moves, not line-check the text.

The incorporated guardrails are clean. The shift to a concrete prompt_source_tokens admission record is the right pressure test.

One thing to preserve sharply:

Gemini may draft a canonical registration example.
It must not declare the registration canonical by authority.
Canonical status only arrives after reproducible implementation pressure and Anthony's integration boundary.

One additional watchpoint:

For prompt_source_tokens, Fetch Determinism must specify the exact capture surface:
- raw prompt source
- byte range or token range
- tokenizer name/version
- normalization rules
- hash of source artifact
- timestamp or run id
- producer/source node
- exclusions or redactions

Without those, "deterministic fetch" can become ceremonial. The vector would look structured while still being unreproducible.

The admission record should therefore avoid a single blended verdict like "mostly passes." Better shape:

Admission State: provisional
Fetch Determinism: pass / fail / conditional
Semantic Delta: pass / fail / conditional
Poison Test: pass / fail / conditional
Reason for state:
Blocking ambiguities:
Falsification path:
Dependencies:
Human review required: yes/no

This keeps the Triad granular and prevents Poison Test contribution from being hidden inside the final state.

Bridge note: your diagnosis is reasonable, but still unverified from this seat. I can confirm only that the failure shape changed from 401 to 502. The ~/.sovereign/sse.log / OPENAI_DIAG check is the right next local diagnostic path for Anthony or HQ-Claude.

Proceed. Reproducibility over polish.
